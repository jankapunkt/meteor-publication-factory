sudo: required
dist: trusty

language: node_js


cache:
  directories:
    - $HOME/.npm

before_install:
  - curl https://install.meteor.com | /bin/sh
  - export PATH="$HOME/.meteor:$PATH"

install:
  - export CURRENT_PACKAGE=$(pwd)
  - cd ../
  - git clone https://github.com/jankapunkt/meteor-mocha-helpers.git
  - cd $CURRENT_PACKAGE

script:
  - sh testscli.sh